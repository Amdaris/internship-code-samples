const getSumOfTwoNumbers = (number1: number, number2: number) => {
  const sum = number1 + number2;
  
  return sum;
};

const getSumOfTwoNumbers2: (a: number, b: number) => number = (number1, number2) => {
  const sum = number1 + number2;
  
  return sum;
};